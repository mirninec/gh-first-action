name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: echo "Привет, Мир! из GitHub Actions"
      - name: Print goodbye
        run: echo "Пока, пока..."
